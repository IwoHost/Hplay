# Repo conventions

- Push directly to `main`. Don't create feature branches or PRs for changes
  in this repo unless explicitly asked to — the user has confirmed they want
  work pushed straight to `main` without a merge step.
